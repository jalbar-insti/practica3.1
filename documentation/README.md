# Documentació

Aquí es troba la documentació generada amb JavaDoc.

Per generar la documentació HTML, executar:

```bash
mvn javadoc:javadoc -DdestDir=documentation/javadoc
```

Els fitxers creats es troben a `documentation/javadoc`.
